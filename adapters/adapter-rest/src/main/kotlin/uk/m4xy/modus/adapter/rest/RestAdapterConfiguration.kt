package uk.m4xy.modus.adapter.rest

import org.springframework.context.annotation.Configuration

/**
 * Provisional Spring wiring for the REST adapter.
 *
 * The real controllers, the /domains/{domainId} routing and the SSE/WebSocket
 * streaming endpoints are owned by a later work item.
 */
@Configuration
public class RestAdapterConfiguration
